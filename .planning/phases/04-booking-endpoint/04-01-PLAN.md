---
phase: 04-booking-endpoint
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/book.test.js
autonomous: true
requirements:
  - BOOK-01
  - BOOK-02
  - BOOK-03

must_haves:
  truths:
    - "tests/book.test.js exists and runs with node --test tests/book.test.js"
    - "Every test case fails with assert.fail() in RED state — not a skip or missing test"
    - "Test file covers all 13 behaviors from the Phase 4 requirement map"
  artifacts:
    - path: "tests/book.test.js"
      provides: "RED test stubs for BOOK-01, BOOK-02, BOOK-03"
      min_lines: 80
  key_links:
    - from: "tests/book.test.js"
      to: "api/book.js"
      via: "import handler from '../api/book.js'"
      pattern: "import handler from"
---

<objective>
Create the RED test scaffold for `POST /api/book` covering all 13 test cases from the research requirement map. Every test must fail with `assert.fail('RED — not yet implemented')` (not a syntax error, not a skip). This is the Wave 0 anchor that locks the contract before implementation.

Purpose: Establish the full behavioral contract for api/book.js before writing a single line of implementation. Forces clear thinking about edge cases (expired quotes, payment failures, upsell validation) before the code exists.
Output: tests/book.test.js in RED state. node --test tests/book.test.js exits non-zero with one failure per stub.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-booking-endpoint/04-RESEARCH.md

<interfaces>
<!-- Key contracts the executor needs. Do NOT explore the codebase. -->

From tests/quote.test.js (established test pattern — mirror this structure exactly):
- Import: `import { describe, it, mock } from 'node:test'`
- Import: `import assert from 'node:assert/strict'`
- Import: `import handler from '../api/book.js'`
- mockReqRes helper returns `{req: {method: 'POST', headers: {}, body}, res}` with res having `_status`, `_body`, `status()`, `json()`, `setHeader()`, `end()`
- mockResponse helper: `{status, ok: status >= 200 && status < 300, headers: {get: () => null}, json: async () => body}`
- tokenResp helper: `mockResponse(200, {access_token: 'test-token', expires_in: 86400})`
- mock.method(globalThis, 'fetch', mockFetch) — always followed by mock.restoreAll() after await handler()

Constants to define at top of file:
- FALLBACK_URL = 'https://svpartners.guestybookings.com/en/properties/693366e4e2c2460012d9ed96'
- BEAPI_TOKEN_URL = 'https://booking.guesty.com/oauth2/token'

From lib/upsells-config.js (valid upsell IDs for test data):
- 'early_checkin' ($75), 'late_checkout' ($50), 'airport_shuttle_to' ($100),
  'airport_shuttle_from' ($100), 'stocked_fridge' ($150)

From lib/notify.js (sendUpsellNotification signature — needed to spy on it):
- export async function sendUpsellNotification({ guest, checkIn, checkOut, confirmationCode, upsells })
- Never throws. Fire-and-forget.

From 04-RESEARCH.md (Guesty instant-book response shape):
- { _id: 'res-abc123', status: 'confirmed', confirmationCode: 'SCR-99999', platform: 'direct' }

Inbound request body shape (per CONTEXT.md locked decision):
- { quoteId, ratePlanId, ccToken, checkIn, checkOut, guest: { firstName, lastName, email, phone }, upsells: [] }
- checkIn and checkOut required because sendUpsellNotification() needs them
</interfaces>
</context>

<feature>
  <name>POST /api/book — RED test stubs</name>
  <files>tests/book.test.js</files>
  <behavior>
Write tests/book.test.js with these 13 test stubs. Each test body must contain ONLY:
  assert.fail('RED — not yet implemented');

Do NOT write any handler calls, mock setup, or assertions yet. The file must be syntactically valid so node:test can parse and run it — but every test fails.

Group into four describe() blocks:

describe('Input Validation (api/book.js)', () => {
  it('BOOK-01: missing quoteId returns 400 with fallbackUrl', ...)
  it('BOOK-01: missing ccToken returns 400 with fallbackUrl', ...)
  it('BOOK-01: ccToken without pm_ prefix returns 400 with fallbackUrl', ...)
  it('BOOK-01: missing guest.firstName returns 400 with fallbackUrl', ...)
  it('BOOK-01: missing guest.email returns 400 with fallbackUrl', ...)
  it('BOOK-03: unknown upsell ID returns 400 with fallbackUrl', ...)
});

describe('Successful Booking (api/book.js)', () => {
  it('BOOK-01, BOOK-02: valid POST body returns 200 with reservationId, confirmationCode, status confirmed', ...)
  it('BOOK-02: success response includes upsells array and upsellTotal', ...)
});

describe('Error Mapping (api/book.js)', () => {
  it('BOOK-01: Guesty 410 returns 410 with code QUOTE_EXPIRED and fallbackUrl', ...)
  it('BOOK-01: Guesty 422 returns 402 with code PAYMENT_DECLINED and fallbackUrl', ...)
  it('BOOK-01: Guesty 500 returns 500 with code BOOKING_FAILED and fallbackUrl', ...)
});

describe('Upsell Processing (api/book.js)', () => {
  it('BOOK-03: upsells present — sendUpsellNotification is called with correct params', ...)
  it('BOOK-03: no upsells — sendUpsellNotification is not called', ...)
});

The file must also define (but not yet use inside test bodies):
- mockReqRes() helper (same pattern as quote.test.js)
- mockResponse() helper
- tokenResp() helper
- validBody constant: { quoteId: 'quote-abc', ratePlanId: 'plan-xyz', ccToken: 'pm_test123', checkIn: '2026-07-01', checkOut: '2026-07-05', guest: { firstName: 'Jane', lastName: 'Doe', email: 'jane@example.com', phone: '555-1234' }, upsells: [] }
- mockBookResponse constant: { _id: 'res-abc123', status: 'confirmed', confirmationCode: 'SCR-99999', platform: 'direct' }

RED commit message: "test(04-01): add failing stubs for book endpoint BOOK-01, BOOK-02, BOOK-03"
  </behavior>
  <implementation>
N/A — this is the RED stub plan only. Implementation happens in 04-02-PLAN.md.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RED test stubs for api/book.js</name>
  <files>tests/book.test.js</files>
  <read_first>
    - tests/quote.test.js — mirror this file's structure exactly (helpers, describe/it nesting, mock patterns)
    - lib/upsells-config.js — copy exact upsell IDs for test data
    - .planning/phases/04-booking-endpoint/04-RESEARCH.md — test map section lists all 13 cases
  </read_first>
  <behavior>
    - Test 1: missing quoteId → assert.fail stub
    - Test 2: missing ccToken → assert.fail stub
    - Test 3: ccToken without pm_ prefix → assert.fail stub
    - Test 4: missing guest.firstName → assert.fail stub
    - Test 5: missing guest.email → assert.fail stub
    - Test 6: unknown upsell ID → assert.fail stub
    - Test 7: valid body → returns 200 with reservationId, confirmationCode, status → assert.fail stub
    - Test 8: success response includes upsells + upsellTotal → assert.fail stub
    - Test 9: Guesty 410 → code QUOTE_EXPIRED → assert.fail stub
    - Test 10: Guesty 422 → code PAYMENT_DECLINED → assert.fail stub
    - Test 11: Guesty 500 → code BOOKING_FAILED → assert.fail stub
    - Test 12: upsells present → sendUpsellNotification called → assert.fail stub
    - Test 13: no upsells → sendUpsellNotification not called → assert.fail stub
  </behavior>
  <action>
Create tests/book.test.js following the quote.test.js structure. File header comment:
// tests/book.test.js
// Requirements: BOOK-01, BOOK-02, BOOK-03
// Run: node --test tests/book.test.js
// Expected state: RED — all tests fail until api/book.js is implemented

Imports:
import { describe, it, mock } from 'node:test';
import assert from 'node:assert/strict';
import handler from '../api/book.js';

Constants:
const FALLBACK_URL = 'https://svpartners.guestybookings.com/en/properties/693366e4e2c2460012d9ed96';
const BEAPI_TOKEN_URL = 'https://booking.guesty.com/oauth2/token';

Helpers (identical structure to quote.test.js):
function mockReqRes(body = {}) — returns {req: {method: 'POST', headers: {}, body}, res} where res has _status=200, _body=null, status(code), json(b), setHeader(k,v), end()
function mockResponse(status, body = {}) — returns {status, ok: status>=200&&status<300, headers: {get: () => null}, json: async () => body}
function tokenResp() — returns mockResponse(200, {access_token: 'test-token', expires_in: 86400})

Test data constants:
const validBody = { quoteId: 'quote-abc', ratePlanId: 'plan-xyz', ccToken: 'pm_test123', checkIn: '2026-07-01', checkOut: '2026-07-05', guest: { firstName: 'Jane', lastName: 'Doe', email: 'jane@example.com', phone: '555-1234' }, upsells: [] };
const mockBookResponse = { _id: 'res-abc123', status: 'confirmed', confirmationCode: 'SCR-99999', platform: 'direct' };

Each test body: assert.fail('RED — not yet implemented');

IMPORTANT: api/book.js does not exist yet. The import will cause a module-not-found error when running — that is expected and acceptable for the RED state. node:test will report the file as a load failure, which satisfies the RED requirement. Do NOT create api/book.js in this plan.
  </action>
  <verify>
    <automated>node --test tests/book.test.js; echo "exit code: $?"</automated>
    The command must exit non-zero (failure). If it exits 0, the stubs are not properly RED.
    Accept: "ERR_MODULE_NOT_FOUND" (api/book.js missing) or individual assert.fail failures — both are valid RED states.
  </verify>
  <done>tests/book.test.js exists with 13 test stubs across 4 describe blocks, all bodies contain assert.fail(), node --test tests/book.test.js exits non-zero</done>
</task>

</tasks>

<verification>
node --test tests/book.test.js exits non-zero (RED state confirmed).
File has 13 it() blocks and 4 describe() blocks.
All imports match established pattern from quote.test.js.
</verification>

<success_criteria>
tests/book.test.js committed in RED state. Wave 2 (04-02-PLAN.md) can proceed to implement api/book.js against these exact test expectations.
</success_criteria>

<output>
After completion, create `.planning/phases/04-booking-endpoint/04-01-SUMMARY.md`
</output>
