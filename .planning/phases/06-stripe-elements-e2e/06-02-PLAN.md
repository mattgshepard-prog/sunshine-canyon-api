---
phase: 06-stripe-elements-e2e
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified: []
autonomous: false
requirements:
  - UI-07
  - UI-08
  - UI-09
  - UI-10

must_haves:
  truths:
    - "A real test booking with Stripe test card 4242424242424242 creates a confirmed reservation in Guesty dashboard"
    - "Step 3 card form renders correctly and the confirm button is disabled until policy checkbox is checked"
    - "A declined test card (4000000000000002) shows an inline error and does not advance"
    - "Step 4 displays the real Guesty confirmation code from the booking"
  artifacts:
    - path: "frontend/checkout-snippet.html"
      provides: "Deployed Step 3 + Step 4 DOM"
      contains: "id=\"card-element\""
    - path: "frontend/js/checkout.js"
      provides: "Full checkout state machine including Steps 3 and 4"
      contains: "initStripeElements"
  key_links:
    - from: "Stripe.js CDN script"
      to: "initStripeElements() Stripe() call"
      via: "window.Stripe global must be available before checkout.js runs"
      pattern: "Stripe\\("
    - from: "Step 3 confirm button"
      to: "Guesty dashboard reservation"
      via: "submitPayment() -> /api/book -> Guesty BEAPI"
      pattern: "api/book"

user_setup:
  - service: stripe
    why: "Stripe publishable key required to initialize Stripe Elements — cannot be obtained by Claude"
    env_vars:
      - name: STRIPE_PUBLISHABLE_KEY
        source: "Sebastian must provide — Stripe Dashboard -> Developers -> API keys -> Publishable key for the Sunshine Canyon connected account"
    dashboard_config: []
  - service: guesty
    why: "End-to-end test requires a real booking to appear in Guesty dashboard"
    env_vars: []
    dashboard_config:
      - task: "Confirm Guesty BEAPI instance is set to instant booking only (no approval required)"
        location: "Guesty Dashboard -> Property Settings -> Booking settings"
      - task: "Confirm at least one manual direct booking has been created so the instant book path is proven open"
        location: "Guesty Dashboard -> Reservations"
---

<objective>
Human end-to-end verification of the complete Phase 6 checkout flow. Requires Sebastian's Stripe publishable key and confirmation that Guesty is configured for instant booking. Claude automated all implementation in Plan 01 — this plan pauses for human testing.

Purpose: Confirm the full guest booking journey works in production — card form renders, PaymentMethod token is created, /api/book fires, confirmation code appears, and the reservation is visible in the Guesty dashboard.
Output: Verified booking confirmation that Phase 6 is production-ready.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-stripe-elements-e2e/06-01-SUMMARY.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Pre-flight — Stripe key and Guesty booking readiness</name>
  <files>none — environment configuration only</files>

  <what-built>
    Plan 01 added Stripe Elements to Step 3, PaymentMethod creation, /api/book integration, and the Step 4 confirmation screen. Everything is code-complete. Two things are needed before the live test can run:
    1. Sebastian's Stripe publishable key set as STRIPE_PUBLISHABLE_KEY in Vercel environment variables
    2. Guesty instance confirmed as instant booking only
  </what-built>

  <action>
    This is a human configuration step. No Claude code changes needed.
    The executor should present the how-to-verify checklist to the user and pause.
  </action>

  <how-to-verify>
    Pre-flight checklist (complete before resuming):

    1. Stripe publishable key
       - Sebastian provides the Stripe publishable key (pk_live_... or pk_test_...) for the Sunshine Canyon connected account
       - Add to Vercel: Dashboard -> sunshine-canyon-api -> Settings -> Environment Variables -> STRIPE_PUBLISHABLE_KEY
       - Redeploy (or trigger a new deployment) so the env var is live

    2. Guesty booking mode
       - Log into Guesty dashboard
       - Navigate to the Sunshine Canyon Retreat property -> Booking settings
       - Confirm "Instant booking" is enabled (no approval required)

    3. Verify /api/payment-info returns the key
       - Visit: https://sunshine-canyon-api.vercel.app/api/payment-info
       - Response should include "stripePublishableKey": "pk_..." (non-null)
       - If still null, the env var is not live yet — redeploy and check again
  </how-to-verify>

  <verify>
    <automated>curl -s https://sunshine-canyon-api.vercel.app/api/payment-info | grep -c stripePublishableKey</automated>
  </verify>

  <done>STRIPE_PUBLISHABLE_KEY is live in Vercel, /api/payment-info returns non-null stripePublishableKey, and Guesty is confirmed as instant booking mode.</done>

  <resume-signal>Type "preflight-done" when STRIPE_PUBLISHABLE_KEY is live in Vercel and /api/payment-info returns a non-null publishableKey, and Guesty is confirmed as instant booking.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end booking test — full checkout flow with real Stripe test card</name>
  <files>none — live site testing only</files>

  <what-built>
    The full 4-step checkout flow:
    Step 1 — price breakdown
    Step 2 — guest details + upsells
    Step 3 — Stripe Elements card form with cancellation policy checkbox + Confirm Booking button
    Step 4 — booking confirmation code, guest details, upsell summary, total
  </what-built>

  <action>
    This is a human verification step. No Claude code changes needed.
    The executor should present the test steps to the user and pause for confirmation.
  </action>

  <how-to-verify>
    Run a complete end-to-end booking using the checkout modal on the live site. Use Stripe test card numbers (if using a test publishable key) or coordinate with Sebastian for a live test:

    **Step-by-step test:**

    1. Open the Sunshine Canyon site and select check-in / check-out dates at least 7 days out
    2. Click "Book Direct" — confirm the drawer opens and Step 1 loads with real pricing from /api/quote
    3. Click "Continue to Book" — confirm Step 2 shows guest form and upsell add-ons load
    4. Fill in test guest details:
       - First: Test, Last: Guest, Email: test@example.com, Phone: +13035551234
    5. Select at least one upsell add-on (confirm running total updates)
    6. Click "Continue to Payment" — confirm Step 3 shows:
       - Charge notice: "$50 today. Remaining $X charged 14 days before check-in"
       - Stripe Card Element renders (not blank, not broken)
       - "Confirm Booking" button is DISABLED
    7. Check the cancellation policy checkbox — confirm "Confirm Booking" becomes ENABLED
    8. Enter Stripe test card: 4242 4242 4242 4242, any future expiry, any CVC
    9. Click "Confirm Booking" — spinner should show "Processing payment..."
    10. Step 4 should appear with:
        - Green checkmark icon
        - "Booking Confirmed!" heading
        - Real confirmation code (e.g., SCR-XXXXXXXX)
        - Guest name, check-in, check-out
        - Upsell selections with prices
        - Grand total
        - "A confirmation email has been sent to test@example.com"
    11. Verify in Guesty dashboard that the reservation appears under Reservations with correct dates, guest name, and confirmation code
    12. Click "Done" — confirm drawer closes and resets

    **Declined card test (optional but recommended):**
    - Re-open checkout with same dates
    - Go through to Step 3
    - Enter Stripe declined card: 4000 0000 0000 0002
    - Click Confirm — expect inline error below card element: "Your card was declined."
    - Confirm button re-disables (policy checkbox remains unchecked after reset)
    - No Guesty reservation created

    **Fallback test (if you want to verify it still works):**
    - Temporarily remove STRIPE_PUBLISHABLE_KEY from Vercel env vars, redeploy
    - Open checkout -> go to Step 3 -> fallback message + "Complete Booking" portal link should show
    - Restore the key afterwards
  </how-to-verify>

  <verify>
    <automated>MISSING — end-to-end test requires human to open the live site and complete a test booking</automated>
  </verify>

  <done>A real Guesty reservation exists in the dashboard with correct guest details and dates. Confirmation code on Step 4 matches the Guesty record. Declined card shows inline error without advancing.</done>

  <resume-signal>Type "e2e-passed" and describe what you saw on Step 4 (confirmation code, any issues). Or describe failures with exact step where it broke.</resume-signal>
</task>

</tasks>

<verification>
Phase 6 is complete when:
- /api/payment-info returns non-null stripePublishableKey in production
- Step 3 renders Stripe Card Element and requires policy checkbox before confirm
- A test booking creates a real confirmed reservation in the Guesty dashboard
- Step 4 displays the Guesty confirmation code
- Declined card shows inline error without advancing
- "Done" button closes and resets the drawer
</verification>

<success_criteria>
- Real Guesty reservation with correct dates and guest details appears in dashboard after test booking
- Confirmation code displayed on Step 4 matches the Guesty reservation record
- Inline card error shown for declined test card (4000000000000002)
- Full mobile test on iPhone: all steps usable, card element renders, confirm flow completes
</success_criteria>

<output>
After completion, create `.planning/phases/06-stripe-elements-e2e/06-02-SUMMARY.md` using the summary template.
</output>
